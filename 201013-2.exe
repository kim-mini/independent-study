#include <stdio.h>
#pragma warning(disable:4996)
#include <stdlib.h>
#include <string.h>
#include<time.h>


//개인 성적 학생성명, 국어, 영어, 수학 세과목의 성적을 입력받는다.
//개인성적입력, 개인성적수정, 전체성적출력, 종료
typedef struct student {
	char* name;
	Sung *juck;
}Student;

typedef struct sung {
	int sungjuck[5];
}Sung;


void insert_grup(Student* man, int hms);





main() 
{
	menu();

	int hms = 0;
	printf("학생 수를 입력해 주세요");
	scanf("%d", &hms);
	insert_grup( hms);

}

void insert_grup(int hms) {

	Student std = (Student*)mallock(sizeof(Student)*hms);
	for (int i = 0; i < hms; i++) {
		man.name = (char*)malloc(sizeof(char)*11);
		man.juck->sungjuck = (int**)malloc(sizeof(int)*5);
	}
		
	
}





void menu(void) {
	puts("☆☆☆☆☆성적보관소☆☆☆☆☆");
	puts("==============================");
	puts("1. 개인 성적입력");
	puts("2. 개인 성적수정");
	puts("3. 전체성적 출력");
	puts("0.종료");
	puts("==============================\n\n");
}

void choice_num(Student *man, int click) {
	

	switch(click) {
	case 1:
	  printfi("학생의 이름을 입력해주세요");
	  scanf("%s", man->name);
 

       for (int j = 0; j < 3; j++) {
           printf("국어, 영어, 수학성적을 입력해주세요");
           scanf("%d", man->juck->sungjuck[j]);	
    	}
	case 2:
	case 3:
	   
	case 0:
	   printf("안녕히 가세요");
}


void view_all(Student men, int hms){
	for(int i = 0; i < hms; i++){
		printf("");
	}
}
